package time_convert

import (
	"fmt"
	"testing"
)

func Test_TimeConvert(t *testing.T) {
	fmt.Println(TimeConvert(126))
	fmt.Println(TimeConvert(76))
	fmt.Println(TimeConvert(20))
	fmt.Println(TimeConvert(120))
}
